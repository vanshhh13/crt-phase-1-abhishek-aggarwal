//problem 1  if staement
#include <iostream>
using namespace std;

int main() {
    int age;
    cin>>age;
    
    if(age>=18){
        cout<<"Eligible for vote..";
        }else{
            cout<<"not Eligible for vote...";
        
        
    }
    return 0;
}
